<% flash.each do |name, msg| %>
    <div class="ui <%= name == "notice" ? "success" : "negative" %> message">
        <i class="close icon"></i>
        <div class="header">
            <%= msg %>
        </div>
    </div>
<% end %>